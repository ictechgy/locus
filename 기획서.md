# breadcrumb (가칭) — UI 요소와 소스 코드를 잇는 트레이스빌리티 맵

- **작성일**: 2026-09-05
- **목적**: iOS 앱의 접근성 트리 요소 ↔ 소스 심볼 대응을 정적(SwiftSyntax + IndexStoreDB)과 동적(시뮬레이터 a11y 트리 덤프) 양쪽에서 구축해, 에이전트·개발자에게 양방향 질의("이 요소는 어느 줄에서 태어났나" / "이 뷰 수정이 무엇에 영향 주나")를 제공하는 오픈소스 기획
- **선점 여부 검증**: 2026-09-05 웹 검색. UI 자동화 에이전트([Arbigent](https://github.com/takahirom/arbigent), mobile-mcp, [Agent Device](https://www.callstack.com/blog/agent-device-ai-native-mobile-automation-for-ios-android))는 **드라이브만 하고 코드 연결이 없음**. 가장 가까운 존재는 학술 시제품 [FixAlly](https://arxiv.org/html/2408.03827v1)(identifier→source 탐색을 a11y 수정 제안에 한정해 구현 — 접근 자체는 선점). [XcodeSelectiveTesting](https://github.com/mikeger/XcodeSelectiveTesting)은 모듈/타깃 단위 임팩트, Serena는 심볼 네비게이션(UI 트리 개념 없음), [AccessibilitySnapshot](https://github.com/cashapp/AccessibilitySnapshot)은 스냅샷 회귀 테스트(소스 매핑 아님). 빈 땅 등급 **⭐⭐☆** — 하향 사유: identifier→source 아이디어의 학술 선점, 상향 사유: 유지되는 개발자 툴·에이전트 인터페이스·양방향(역참조) 계층은 확인상 공백

---

## 한 줄 정의

> 에이전트는 화면의 버튼을 볼 수 있지만, 그 버튼이 어느 줄에서 태어났는지 모른다. breadcrumb는 접근성 트리의 모든 요소에 출처를 단다 — "이 스위치는 SettingsView.swift:142의 Toggle이고, 이 식별자를 쓰는 UI 테스트는 3개다."

---

## 문제 정의: 두 종류의 에이전트, 같은 단절

1. **UI 자동화 에이전트의 한계** — 스크린샷·a11y 트리를 보고 탭하고 스와이프할 수 있으나, 버그를 *봐도* 어느 소스를 고치는지 모른다. 발견과 수정 사이의 손수레 짐 나르기(스크린샷을 사람에게 보여주고 파일명을 듣고 다시 시키기)
2. **코딩 에이전트의 한계** — SwiftUI 뷰를 수정해도 어떤 화면·요소·UI 테스트에 영향 주는지 모른다. 검증 수단이 "전체 빌드 + 전체 UI 테스트"뿐이라 비용이 커서 회귀를 놓친다 (yield-audit verification gap의 직접 원인)
3. **identifier 부채** — `accessibilityIdentifier`가 문자열로 코드·테스트·자동화 스크립트 3곳에 흩어져 오탈자가 조용히 실패한다
4. **미식별 요소의 무감각** — 자동화 불가능한 요소(identifier 없음)가 화면에 얼마나 있는지 아는 팀이 없다

---

## 데이터 모델: 맵 항목 1개 = YAML 1개 (정적 원장 + 동적 스냅샷 결합)

```yaml
element:
  key: acc://SettingsScreen/notifications-toggle
  identifier: notifications.toggle       # 런타임 접근성 식별자
  label: "알림 허용"
  source:                                # 정적 크롤의 앵커
    file: Features/Settings/SettingsView.swift
    line: 142
    symbol: SettingsView.notificationsToggle   # IndexStoreDB 심볼
  screen: SettingsScreen                 # 내비게이션 경계 유추 (v0.3, 확신도 첨부)
  tests: [Tests/SettingsUITests.swift:31]      # 식별자 문자열 역색인
  dynamic:
    last_seen: 2026-09-05-sim-18         # 시뮬레이터 스냅샷에서 확인
    matched_by: identifier               # identifier | label+type 휴리스틱
confidence: high                         # 정적 직매칭 > 라벨 휴리스틱 > 동적 단독
```

설계 결정:

- **정적 원장이 본체, 동적 스냅샷은 보강** — 동적 덤프 없이도 `where_is`/`what_renders`가 성립해야 콜드스타트가 없다
- **confidence 필수** — SwiftUI의 동적 특성상 정적 추출엔 누락이 있고, 화면 경계는 유추다. 단정 대신 확신도
- **미매칭 잔차는 버그가 아니라 기능** — 식별자 없는 요소·정적 추출 실패 요소의 리포트가 그 자체로 자동화 부채 계량기

---

## 워크플로: 크롤 → 스냅샷 → 매칭 → 질의

| 단계 | LLM | 내용 |
|---|---|---|
| 정적 크롤 | 불필요 | SwiftSyntax로 `accessibilityIdentifier`·`accessibilityLabel` 할당, SwiftUI 뷰 타입·body 구조 추출 → IndexStoreDB로 심볼 앵커 |
| 역색인 | 불필요 | 테스트·자동화 코드 내 identifier 문자열 리터럴 사용처 → 요소별 tests 역참조 |
| 동적 스냅샷 | 불필요 | XcodeBuildMCP/idb 경유 시뮬레이터 a11y 트리 덤프 → 정적 원장과 매칭(식별자 직매칭 → 라벨+타입 휴리스틱) |
| 잔차 리포트 | 옵션 | 미식별 요소·미매칭 요소 목록. 화면별 자동화 커버리지 점수 |
| 질의 | 불필요 | MCP 툴: `where_is(element)`, `what_renders(symbol\|file)`, `affected_tests(diff)`, `unidentified_elements(screen)` |

---

## 제품 원칙 (context-guard·yield-audit·tombstone의 문법 계승)

- **완전 로컬 기본**: 크롤·덤프·매칭 전부 로컬. 시뮬레이터 있으면 충분, 실기기 불요
- **결정적 우선**: 매칭은 식별자 직매칭(결정적) → 휴리스틱(확신도 표기) 순서. LLM 불요
- **단정 금지**: 화면 경계·소스 대응은 확신도로 표현. "이 요소는 틀림없이 이 줄"이라는 주장을 하지 않는다
- **문자열의 단일 출처 유도**: identifier 코드젠은 v1.x 옵션 — 강제가 아니라 "역색인이 오탈자를 잡아준다"는 가치로 유도

---

## 아키텍처 (v0.3 기준)

```
breadcrumb crawl (정적, 본체)              breadcrumb query (MCP / CLI)
┌───────────────────────────────┐          ┌───────────────────────────────┐
│ SwiftSyntax  식별자·라벨·뷰 구조 │          │ where_is(element)              │
│ IndexStoreDB 심볼 앵커·참조     │  map/    │ what_renders(symbol|file)      │
│ 역색인        테스트 내 리터럴  │ *.yaml ─▶│ affected_tests(diff)           │
└───────────────────────────────┘          │ unidentified_elements(screen)  │
┌───────────────────────────────┐          └───────────────────────────────┘
│ snapshot (동적, 보강)          │          저장: 완전 로컬. 언어: Swift
│ 시뮬레이터 a11y 덤프 → 매칭     │ ──────▶  (SwiftSyntax/indexstore-db와
│ 잔차 리포트(미식별·미매칭)      │          동일 런타임 — 제작자 역량 직결)
└───────────────────────────────┘
```

---

## 로드맵

```
v0.1  정적 크롤러(SwiftSyntax 식별자·라벨 추출 + IndexStoreDB 심볼 앵커)
      + CLI where_is / what_renders — 동적 덤프 없이 성립하는 최소 원장
v0.2  테스트·자동화 코드 역색인 + affected_tests(diff)
      ← XcodeSelectiveTesting이 못 하는 심볼→요소 단위 임팩트. 검증 비용 절감이
        측정 가능한 첫 가치(전체 UI 테스트 → 영향 3개)
v0.3  동적 스냅샷 결합(시뮬레이터 덤프 매칭, 잔차 리포트) + Claude Code MCP 4툴
v1.x  화면 경계 유추(내비게이션 구조), identifier 코드젠(문자열 부채 소멸),
      Android 확장(Kotlin/Compose — 제작자의 크로스플랫폼 정적분석 자산 재사용)
```

---

## 차별화·경계 선언 (README에 명시할 것)

| 인접 | 그들과의 차이 |
|---|---|
| **Arbigent / mobile-mcp / Agent Device** | UI 드라이브 자동화. 코드 연결 없음 — 경쟁이 아니라 **상위 계층 소비자**(드라이브하다 발견한 요소를 where_is로). 상호운용을 전략으로 |
| **FixAlly (학술)** | identifier→source 탐색을 a11y 자동수정에 한정한 시제품. 유지되는 도구가 아니며 역참조·테스트 역색인·에이전트 인터페이스 없음 — 정직하게 선행 연구로 인용 |
| **XcodeSelectiveTesting** | 타깃/모듈 의존성 그래프 단위 선택 실행. 우리는 심볼→UI 요소→테스트 단위 |
| **Serena / SourceKit-LSP MCP** | 소스 심볼 네비게이션. UI 트리·런타임 요소의 개념이 없음 — 정적 축에서 접점, UI 축에서 독립 |
| **AccessibilitySnapshot (Cash App)** | 접근성 계층의 스냅샷 회귀 테스트. 소스 매핑·임팩트 질의 없음 |
| **Accessibility Inspector** | 런타임 단일 요소 뷰어. 원장·역색인·질의 인터페이스 없음 |
| **coroner (포트폴리오)** | coroner는 프로덕션 증거 유입, breadcrumb는 코드↔UI 대응. 부검으로 찾은 크래시 프레임의 화면 귀속에 breadcrumb가 쓰일 수 있는 접점 |

---

## 리스크와 대응

- **SwiftUI 동적 특성**(조건부 뷰·ViewBuilder 전개·동적 문자열 라벨)으로 정적 추출 누락 → 누락을 숨기지 말고 **잔차 리포트**로 승화 + 동적 스냅샷 보강 + confidence 표기
- **identifier 관행 없는 팀**(애초에 식별자가 거의 없음) → 미식별 리포트가 도입 동기가 되게 설계(커버리지 점수). v0.1은 식별자 문화가 있는 팀 타깃
- **애플 흡수 리스크**(Xcode/ACP가 런타임 뷰어 계층 제공 가능성) → Xcode가 줄 수 있는 건 런타임 뷰잉까지. **정적 원장 + 테스트 역색인 + diff 임팩트**는 Xcode 스코프 밖
- **SwiftSyntax/macOS 버전 추종 비용** → indexstore-db·SwiftSyntax를 동일 런타임(Swift)에서 쓰는 구조로 도구 사슬 단순화
- **과잉 신뢰**(에이전트가 low confidence 매칭을 확정으로 소비) → MCP 응답에 confidence 강제 노출, low는 "후보"로만 반환

---

## 이름 후보

`breadcrumb`는 "UI에서 코드로 돌아가는 길"이라는 기능 서사와 직관적으로 정합. 후보:

1. **`breadcrumb`** (범용어라 검색 오염 중간 — "breadcrumb UI 패턴"과의 경합 확인 필요)
2. `locus` (소재지 — 짧고 중복 적으나 기능 연상이 약함)
3. `signpost`는 Apple `os_signpost`와 정면 충돌, 배제

저장소 개설 시 GitHub/SPM/Homebrew 가용성 확인 후 확정.

---

## 첫 공개 전략

데모 GIF 한 편이 전부다: **에이전트가 시뮬레이터에서 버그 발견 → `where_is`로 소스 즉시 오픈 → 수정 → `affected_tests`로 관련 3개 테스트만 재실행 → 통과.** 전체 UI 테스트 실행과의 소요·토큰 대조를 같은 화면에. 후킹 한 줄: "에이전트에게 화면과 코드 사이의 지도를 줘라." 식별자 문화가 있는 팀을 첫 타깃으로, AccessibilitySnapshot·Arbigent 커뮤니티에 상호운용 어댑터로 접근.

**포트폴리오 내 위치**: context-guard(입력 위생) → yield-audit(운명 측정) → **breadcrumb(코드↔UI 대응 — 정적분석 역량이 제품 그 자체가 되는 지점)** → coroner·tombstone. "에이전트×모바일" 축에서 유일하게 정적분석이 본체인 기둥.
